// default.tsx renders if a slot can't get data from url params when on a different url after refresh
const DefaultComplexDashboardPage = () => {
  return <h1>Default Complex Dashboard</h1>;
};

export default DefaultComplexDashboardPage;

import Card from "@/app/components/card";

// Default files display when hardcoding url.
const DefaultRevenueMetrics = () => {
  return <Card>Default Revenue Metrics</Card>;
};

export default DefaultRevenueMetrics;

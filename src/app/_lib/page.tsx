// Create private folders by adding an underscore (_) at the start of the file name.
// Use "%5F" if you want a public folder with an underscore at the start of the file name.
// You can also just put the file outside of the app folder or rename it to something that is not page.tsx
const PrivateRoute = () => {
  return <h1>Private Folder</h1>;
};

export default PrivateRoute;

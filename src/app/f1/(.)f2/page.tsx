// (.) Means target file is 1 folder above
// Intercepted pages are displayed unless the user refreshes the page or hardcodes the url.
const InterceptedF2 = () => {
  return <h1>(.) InterceptedF2 Page</h1>;
};

export default InterceptedF2;
